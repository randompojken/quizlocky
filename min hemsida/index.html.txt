html

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>QuizLocky — Earn Your Screen Time</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --purple: #6B5EE4;
    --purple-light: #8B7FF0;
    --purple-dark: #4A3FC4;
    --purple-bg: #F0EEFF;
    --purple-soft: #E8E4FF;
    --text: #1A1528;
    --text-muted: #6B6580;
    --white: #FFFFFF;
    --bg: #FAFAF8;
    --card-bg: #FFFFFF;
    --border: rgba(107, 94, 228, 0.15);
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg);
    color: var(--text);
    overflow-x: hidden;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    padding: 1.2rem 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(250,250,248,0.85);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }
  .nav-logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.4rem;
    color: var(--purple);
    display: flex;
    align-items: center;
    gap: 8px;
    text-decoration: none;
  }
  .nav-logo span { color: var(--text); }
  .nav-cta {
    background: var(--purple);
    color: white;
    padding: 0.6rem 1.4rem;
    border-radius: 50px;
    font-weight: 500;
    font-size: 0.9rem;
    text-decoration: none;
    transition: background 0.2s, transform 0.2s;
  }
  .nav-cta:hover { background: var(--purple-dark); transform: translateY(-1px); }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 2rem 4rem;
    position: relative;
    overflow: hidden;
  }
  .hero-bg-circle {
    position: absolute;
    border-radius: 50%;
    background: var(--purple-soft);
    filter: blur(80px);
    pointer-events: none;
  }
  .hero-bg-circle.c1 { width: 500px; height: 500px; top: -100px; left: -150px; opacity: 0.5; }
  .hero-bg-circle.c2 { width: 400px; height: 400px; bottom: -100px; right: -100px; opacity: 0.4; }
  .hero-bg-circle.c3 { width: 300px; height: 300px; top: 40%; left: 50%; transform: translate(-50%,-50%); opacity: 0.3; }

  .hero-content { position: relative; z-index: 2; max-width: 800px; }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--purple-soft);
    color: var(--purple-dark);
    padding: 0.4rem 1rem;
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(107,94,228,0.2);
  }
  .badge-dot { width: 6px; height: 6px; background: var(--purple); border-radius: 50%; animation: pulse 2s infinite; }
  @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.5;transform:scale(1.3)} }

  .hero h1 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2.8rem, 6vw, 5rem);
    font-weight: 800;
    line-height: 1.05;
    margin-bottom: 1.5rem;
    color: var(--text);
    letter-spacing: -0.02em;
  }
  .hero h1 .accent { color: var(--purple); }

  .hero p {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: var(--text-muted);
    max-width: 560px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
    font-weight: 400;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--purple);
    color: white;
    padding: 0.9rem 2rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.2s;
    box-shadow: 0 8px 30px rgba(107,94,228,0.35);
  }
  .btn-primary:hover { background: var(--purple-dark); transform: translateY(-2px); box-shadow: 0 12px 40px rgba(107,94,228,0.45); }
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: white;
    color: var(--text);
    padding: 0.9rem 2rem;
    border-radius: 50px;
    font-weight: 500;
    font-size: 1rem;
    text-decoration: none;
    border: 1.5px solid var(--border);
    transition: all 0.2s;
  }
  .btn-secondary:hover { border-color: var(--purple); color: var(--purple); transform: translateY(-2px); }

  .hero-mascot {
    margin: 4rem auto 0;
    width: 200px;
    height: 200px;
    background: var(--purple-soft);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 7rem;
    animation: float 4s ease-in-out infinite;
    border: 3px solid rgba(107,94,228,0.2);
    position: relative;
  }
  @keyframes float { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-12px)} }

  .hero-stats {
    display: flex;
    gap: 3rem;
    justify-content: center;
    margin-top: 3rem;
    flex-wrap: wrap;
  }
  .hero-stat { text-align: center; }
  .hero-stat .num {
    font-family: 'Syne', sans-serif;
    font-size: 2rem;
    font-weight: 800;
    color: var(--purple);
  }
  .hero-stat .label { font-size: 0.85rem; color: var(--text-muted); margin-top: 2px; }

  /* SECTIONS */
  section { padding: 6rem 2rem; }
  .container { max-width: 1100px; margin: 0 auto; }
  .section-tag {
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--purple);
    margin-bottom: 0.75rem;
  }
  .section-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    line-height: 1.1;
    color: var(--text);
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .section-sub {
    font-size: 1.05rem;
    color: var(--text-muted);
    max-width: 520px;
    line-height: 1.7;
  }

  /* HOW IT WORKS */
  .how-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
    margin-top: 3.5rem;
  }
  .how-card {
    background: var(--card-bg);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 2rem;
    position: relative;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .how-card:hover { transform: translateY(-4px); box-shadow: 0 16px 40px rgba(107,94,228,0.12); }
  .step-num {
    width: 40px;
    height: 40px;
    background: var(--purple-soft);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1rem;
    color: var(--purple);
    margin-bottom: 1.2rem;
  }
  .how-card h3 {
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
    color: var(--text);
  }
  .how-card p { font-size: 0.95rem; color: var(--text-muted); line-height: 1.6; }

  /* FEATURES */
  .features-section { background: var(--purple-bg); }
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 3.5rem;
  }
  .feature-card {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    border: 1px solid var(--border);
    transition: transform 0.2s;
  }
  .feature-card:hover { transform: translateY(-3px); }
  .feature-icon {
    width: 52px;
    height: 52px;
    background: var(--purple-soft);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.6rem;
    margin-bottom: 1.2rem;
  }
  .feature-card h3 {
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    font-size: 1.05rem;
    margin-bottom: 0.5rem;
    color: var(--text);
  }
  .feature-card p { font-size: 0.92rem; color: var(--text-muted); line-height: 1.6; }

  /* WIN WIN */
  .winwin {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-top: 3.5rem;
  }
  @media(max-width: 640px) { .winwin { grid-template-columns: 1fr; } }
  .winwin-card {
    border-radius: 24px;
    padding: 2.5rem;
    position: relative;
    overflow: hidden;
  }
  .winwin-card.left { background: var(--purple); color: white; }
  .winwin-card.right { background: white; border: 1.5px solid var(--border); }
  .winwin-card h3 {
    font-family: 'Syne', sans-serif;
    font-size: 1.4rem;
    font-weight: 800;
    margin-bottom: 1rem;
  }
  .winwin-card.left h3 { color: white; }
  .winwin-card.right h3 { color: var(--text); }
  .winwin-card p { font-size: 0.95rem; line-height: 1.7; }
  .winwin-card.left p { color: rgba(255,255,255,0.8); }
  .winwin-card.right p { color: var(--text-muted); }
  .winwin-emoji { font-size: 2.5rem; margin-bottom: 1rem; display: block; }

  /* PRICING */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 3.5rem;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }
  .price-card {
    background: white;
    border: 1.5px solid var(--border);
    border-radius: 24px;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: transform 0.2s;
  }
  .price-card:hover { transform: translateY(-4px); }
  .price-card.featured { border-color: var(--purple); border-width: 2px; position: relative; }
  .popular-badge {
    position: absolute;
    top: -14px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--purple);
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    padding: 4px 14px;
    border-radius: 50px;
    white-space: nowrap;
  }
  .price-label { font-size: 0.85rem; color: var(--text-muted); font-weight: 500; margin-bottom: 0.5rem; }
  .price-amount {
    font-family: 'Syne', sans-serif;
    font-size: 3rem;
    font-weight: 800;
    color: var(--text);
    line-height: 1;
    margin-bottom: 0.25rem;
  }
  .price-period { font-size: 0.85rem; color: var(--text-muted); margin-bottom: 1.5rem; }
  .price-features { list-style: none; text-align: left; margin-bottom: 2rem; }
  .price-features li {
    font-size: 0.9rem;
    color: var(--text-muted);
    padding: 0.4rem 0;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .price-features li::before { content: "✓"; color: var(--purple); font-weight: 700; }
  .price-btn {
    display: block;
    width: 100%;
    padding: 0.85rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    text-align: center;
    transition: all 0.2s;
    cursor: pointer;
  }
  .price-btn.primary { background: var(--purple); color: white; }
  .price-btn.primary:hover { background: var(--purple-dark); }
  .price-btn.secondary { border: 1.5px solid var(--border); color: var(--text); background: transparent; }
  .price-btn.secondary:hover { border-color: var(--purple); color: var(--purple); }

  /* CTA */
  .cta-section {
    background: var(--purple);
    text-align: center;
    padding: 6rem 2rem;
  }
  .cta-section h2 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    color: white;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }
  .cta-section p { color: rgba(255,255,255,0.75); font-size: 1.05rem; max-width: 480px; margin: 0 auto 2.5rem; line-height: 1.7; }
  .btn-white {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: white;
    color: var(--purple);
    padding: 0.9rem 2.2rem;
    border-radius: 50px;
    font-weight: 700;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.2s;
  }
  .btn-white:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(0,0,0,0.15); }

  /* FOOTER */
  footer {
    background: var(--text);
    color: rgba(255,255,255,0.6);
    padding: 3rem 2rem;
    text-align: center;
  }
  .footer-logo {
    font-family: 'Syne', sans-serif;
    font-size: 1.3rem;
    font-weight: 800;
    color: white;
    margin-bottom: 1rem;
    display: block;
  }
  .footer-links {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
    margin: 1.5rem 0;
  }
  .footer-links a { color: rgba(255,255,255,0.5); text-decoration: none; font-size: 0.9rem; transition: color 0.2s; }
  .footer-links a:hover { color: white; }
  .footer-copy { font-size: 0.8rem; color: rgba(255,255,255,0.3); }

  /* ANIMATIONS */
  .fade-up { opacity: 0; transform: translateY(24px); transition: opacity 0.6s, transform 0.6s; }
  .fade-up.visible { opacity: 1; transform: translateY(0); }

  @media(max-width: 768px) {
    nav { padding: 1rem 1.25rem; }
    .hero { padding: 7rem 1.25rem 3rem; }
    section { padding: 4rem 1.25rem; }
    .hero-mascot { width: 160px; height: 160px; font-size: 5.5rem; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">🔒 <span>Quiz</span>Locky</a>
  <a href="#download" class="nav-cta">Download Free</a>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg-circle c1"></div>
  <div class="hero-bg-circle c2"></div>
  <div class="hero-bg-circle c3"></div>
  <div class="hero-content">
    <div class="hero-badge">
      <span class="badge-dot"></span>
      Now available on iOS
    </div>
    <h1>Earn Your<br><span class="accent">Screen Time.</span></h1>
    <p>Want to open TikTok? Finish your flashcards first. QuizLocky turns your phone from your biggest distraction into your study secret weapon.</p>
    <div class="hero-buttons">
      <a href="#download" class="btn-primary">📱 Download on App Store</a>
      <a href="#how" class="btn-secondary">See how it works</a>
    </div>
    <div class="hero-mascot">🔒</div>
    <div class="hero-stats">
      <div class="hero-stat"><div class="num">AI</div><div class="label">Auto-generates questions</div></div>
      <div class="hero-stat"><div class="num">2×</div><div class="label">More study time</div></div>
      <div class="hero-stat"><div class="num">0</div><div class="label">Mindless scrolling</div></div>
    </div>
  </div>
</section>

<!-- HOW IT WORKS -->
<section id="how">
  <div class="container">
    <div class="section-tag">How it works</div>
    <h2 class="section-title">Simple. Smart. Effective.</h2>
    <p class="section-sub">Set it up once, and QuizLocky handles the rest — making sure you study before you scroll.</p>
    <div class="how-grid">
      <div class="how-card fade-up">
        <div class="step-num">1</div>
        <h3>Add your study material</h3>
        <p>Paste in your notes, text, or topics. AI instantly creates personalized flashcards and questions for you.</p>
      </div>
      <div class="how-card fade-up">
        <div class="step-num">2</div>
        <h3>Lock your apps</h3>
        <p>Choose which apps to lock — TikTok, Instagram, YouTube, whatever distracts you most.</p>
      </div>
      <div class="how-card fade-up">
        <div class="step-num">3</div>
        <h3>Study to unlock</h3>
        <p>To open a locked app, you must correctly answer your own study questions first. No cheating.</p>
      </div>
      <div class="how-card fade-up">
        <div class="step-num">4</div>
        <h3>Win either way</h3>
        <p>You scroll less AND study more. Every time you touch your phone, you make progress.</p>
      </div>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section class="features-section">
  <div class="container">
    <div class="section-tag">Features</div>
    <h2 class="section-title">Everything you need to<br>actually study.</h2>
    <div class="features-grid">
      <div class="feature-card fade-up">
        <div class="feature-icon">🤖</div>
        <h3>AI Question Generator</h3>
        <p>Paste your material and AI creates hundreds of smart questions instantly. No more making flashcards by hand.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">🔐</div>
        <h3>App Locking</h3>
        <p>Lock any app on your phone. They stay locked until you prove you've studied. It's that simple.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">🃏</div>
        <h3>Smart Flashcards</h3>
        <p>Study inside the app with beautiful flashcards. The more you get right, the more screen time you earn.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">📊</div>
        <h3>Progress Tracking</h3>
        <p>See how much you've studied, what you've mastered, and how much screen time you've earned this week.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">⚡</div>
        <h3>Instant Setup</h3>
        <p>Add your study material in seconds. AI handles organizing everything so you can focus on learning.</p>
      </div>
      <div class="feature-card fade-up">
        <div class="feature-icon">🎯</div>
        <h3>Your Material, Your Rules</h3>
        <p>Works with any subject — biology, history, languages, code. If you can write it, we can quiz you on it.</p>
      </div>
    </div>
  </div>
</section>

<!-- WIN WIN -->
<section>
  <div class="container">
    <div class="section-tag">The concept</div>
    <h2 class="section-title">A true win-win.</h2>
    <p class="section-sub">Most study apps fight your phone habits. QuizLocky uses them.</p>
    <div class="winwin">
      <div class="winwin-card left fade-up">
        <span class="winwin-emoji">📵</span>
        <h3>Less mindless scrolling</h3>
        <p>By making you earn your screen time, you naturally use your phone less and more intentionally. Your brain gets a break from the dopamine loop.</p>
      </div>
      <div class="winwin-card right fade-up">
        <span class="winwin-emoji">🧠</span>
        <h3>More actual studying</h3>
        <p>Every time you reach for your phone, you study. Not because you forced yourself, but because that's just how it works now. Effortless habit-building.</p>
      </div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section style="background: var(--purple-bg);">
  <div class="container" style="text-align: center;">
    <div class="section-tag">Pricing</div>
    <h2 class="section-title">Start free. Stay focused.</h2>
    <p class="section-sub" style="margin: 0 auto 0;">Try QuizLocky for free and unlock premium when you're ready.</p>
    <div class="pricing-grid">
      <div class="price-card fade-up">
        <div class="price-label">Weekly</div>
        <div class="price-amount">$5.99</div>
        <div class="price-period">per week</div>
        <ul class="price-features">
          <li>Unlimited app locking</li>
          <li>AI question generator</li>
          <li>Smart flashcards</li>
          <li>Progress tracking</li>
        </ul>
        <a href="#download" class="price-btn secondary">Get started</a>
      </div>
      <div class="price-card featured fade-up">
        <div class="popular-badge">Most popular</div>
        <div class="price-label">Annual</div>
        <div class="price-amount">$49</div>
        <div class="price-period">per year — save 84%</div>
        <ul class="price-features">
          <li>Everything in Weekly</li>
          <li>Priority support</li>
          <li>Early access to new features</li>
          <li>Unlimited study decks</li>
        </ul>
        <a href="#download" class="price-btn primary">Get Annual</a>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section" id="download">
  <h2>Ready to take back<br>your focus?</h2>
  <p>Download QuizLocky for free and start turning your screen time into study time today.</p>
  <a href="https://apps.apple.com/app/quizlocky" class="btn-white">📱 Download on App Store</a>
</section>

<!-- FOOTER -->
<footer>
  <span class="footer-logo">🔒 QuizLocky</span>
  <p style="font-size: 0.9rem; max-width: 400px; margin: 0 auto;">Earn your screen time. Study smarter, scroll less.</p>
  <div class="footer-links">
    <a href="/privacy-policy.html">Privacy Policy</a>
    <a href="/terms.html">Terms of Service</a>
    <a href="mailto:hello@quizlocky.site">Contact</a>
    <a href="https://apps.apple.com/app/quizlocky">App Store</a>
  </div>
  <p class="footer-copy">© 2026 QuizLocky. All rights reserved.</p>
</footer>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((e, i) => {
      if (e.isIntersecting) {
        setTimeout(() => e.target.classList.add('visible'), i * 80);
      }
    });
  }, { threshold: 0.1 });
  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
</script>
</body>
</html>